{
  "name": "realtime-chat-app",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:frontend": "turbo run db:generate --filter=@chat/db && turbo run build --filter=@chat/frontend",
    "build:backend": "turbo run db:generate --filter=@chat/db && turbo run build --filter=@chat/backend",
    "build:ws": "turbo run build --filter=@chat/ws",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^1.10.12",
    "prettier": "^3.0.0"
  },
  "packageManager": "npm@8.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
